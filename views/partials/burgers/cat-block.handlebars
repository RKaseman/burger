
<li>

	{{name}}

	<button class="eatStatus" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Eat{{else}}<del>[del]</del>{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}"><del>[del]</del></button>

</li>

