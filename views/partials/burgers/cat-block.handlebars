
<li>

	{{name}}

	<button class="eatStatus" data-id="{{id}}" data-neweat="{{devour}}">
		{{#if devour}}Eat{{else}}<del>[del]</del>{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}"><del>[del]</del></button>

</li>

